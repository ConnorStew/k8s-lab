; This file is merged with inventory-ha.ini in ha mode, so we include the empty groups here so we can declare children.

[control_plane_primary]
k8s-cp ansible_host=192.168.122.10 ansible_user=debian

; Additional control plane nodes - HA only.
[control_plane_secondary]

; The kubeadm control plane endpoint - HA only.
[load_balancer]

[workers]
k8s-w1 ansible_host=192.168.122.11 ansible_user=debian
k8s-w2 ansible_host=192.168.122.12 ansible_user=debian

[control_plane:children]
control_plane_primary
control_plane_secondary

[cluster:children]
control_plane
workers

[all:vars]
ansible_python_interpreter=/usr/bin/python3