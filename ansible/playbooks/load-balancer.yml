---
- name: Install prerequisites
  become: true
  hosts: load_balancer

  tasks:
    - name: Install HAProxy
      ansible.builtin.apt:
        name: haproxy
        state: present
        update_cache: true
